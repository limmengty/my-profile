import type { Metadata } from "next"
import ContactView from "@/views/contact"

export const metadata: Metadata = {
  title: "Contact",
  description: "Get in touch with Lim Mengty.",
}

export default ContactView
